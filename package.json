{
  "name": "draw-app",
  "private": true,
  "scripts": {
     "build": "turbo run build --filter=@repo/http-backend...",
  "start": "pnpm --filter @repo/http-backend run start",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.3",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@10.11.1",
  "engines": {
    "node": ">=18"
  }
}
